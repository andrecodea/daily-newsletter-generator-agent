# daily-newsletter-generator-agent
This is an n8n agent that compiles data from the most 5 recent news in a specific domain via Brave Search, generates a newsletter in HTML and sends it to the user via Telegram.
